* Implemented a student-t likelihood which handles outliers much better than a gaussian one (with the Gaussian case the sampler usually just fails with outliers because the probability for the samples from the outlying posterior goes to zero, which makes the entire likelihood zero in the product)
* Extended to two dimensions, built a demo notebook: sampler/demo_2d.ipynb